package com.restaurants.demo.entity;

/*
    ERole is an enumeration that defines the possible roles for users in the application. It includes two roles: ADMIN and STAFF.
*/
public enum ERole {
    ADMIN,
    STAFF
}